#openssl genpkey -algorithm RSA -out private_key.pem
#openssl req -new -key private_key.pem -out csr.pem
#openssl req -x509 -key private_key.pem -in csr.pem -out self_signed_certificate.pem -days 365
#openssl x509 -text -noout -in self_signed_certificate.pem
openssl x509 -inform PEM -outform DER -in self_signed_certificate.pem -out self_signed_certificate.crt

